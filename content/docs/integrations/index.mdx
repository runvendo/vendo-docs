---
title: Integrations
description: Per-provider pages for every integration Vendo brokers — OpenAI, Anthropic, Telegram, Stripe, Notion, and more.
---

Each integration page covers what the integration is, which auth modes are supported (BYOK / OAuth / shared), how to declare it in `vendo.yaml`, what env vars are exposed at runtime, the proxy endpoint, per-call cost mechanics, and code examples in Python, TypeScript, and Swift.
