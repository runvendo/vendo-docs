---
title: Google Gemini
description: Gemini family of models. Vendo holds the upstream credential and bills per token (including cached input) through the proxy with a route allowlist on /v1beta/models.
category: ai
slug: gemini
logo: https://cdn.vendo.run/integrations/gemini.2487985c.svg
---

{/* Generated by scripts/build-integrations.mjs from packages/integrations/gemini/catalog.json. Hand-edit only the prose block between PROSE-START and PROSE-END markers. */}

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

{/* PROSE-START — hand-edited; the generator preserves this block */}
Drop-in Gemini API. Point the official Google Generative AI SDK at Vendo's proxy and Vendo authenticates the call, debits credits per token, and forwards to Google. No separate Google Cloud project, no separate billing account, no key rotation when you swap apps. Context caching is fully tracked: cached input tokens are metered separately so you can see the savings.
{/* PROSE-END */}

## Auth modes

| Profile | Default | How it works |
| --- | --- | --- |
| `vendo_managed_pool` (Vendo-managed pool) | Yes | Vendo holds the upstream credential and bills per call against the tenant's Vendo balance. No tenant signup with the provider required. |


## Environment variables

These are the env vars Vendo injects into your deployment at boot when this integration is bound.

| Variable | Source |
| --- | --- |
| `GEMINI_API_KEY` | `credential.vendo_sk` |
| `GEMINI_BASE_URL` | `literal:https://gemini-proxy.vendo.run/v1` |


## Reading the credential at runtime

Vendo writes `GEMINI_API_KEY` (and any companion vars above) into your deployment's environment at boot — the official Google Gemini SDK auto-discovers it, so most code just instantiates the client with no extra wiring. `vendo.token(slug)` is the resolution-chain-aware alternative that reads the same value in Vendo mode and falls back to whatever you set in OSS mode; use whichever fits your style. See [Two modes](/docs/concepts/two-modes) for the full resolution chain.

## Proxy endpoint

Calls to Google Gemini are brokered through Vendo's proxy at:

```
https://gemini-proxy.vendo.run
```

Point the official Google Gemini SDK at this base URL; Vendo authenticates, meters per call, and forwards upstream. Your code never sees the upstream credential.

## Quickstart

<Tabs items={["Python", "TypeScript", "Swift"]}>
  <Tab value="Python">
```python
import vendo

# Native Gemini API access through Vendo. Same Google Generative AI SDK, billed via your Vendo balance.
token = vendo.token("gemini")

# Use `token` wherever you would have used the provider's API key —
# pass it to the official SDK or set it as the env var the SDK reads.
print(token)
```
  </Tab>
  <Tab value="TypeScript">
```typescript
import { Vendo } from "@vendodev/sdk";

const vendo = new Vendo();
// Native Gemini API access through Vendo. Same Google Generative AI SDK, billed via your Vendo balance.
const geminiToken = await vendo.token("gemini");

// Use `geminiToken` wherever you would have used the provider's API
// key — pass it to the official SDK or read it as the env var the SDK expects.
console.log(geminiToken);
```
  </Tab>
  <Tab value="Swift">
```swift
import VendoSDK

let vendo = Vendo()
// Native Gemini API access through Vendo. Same Google Generative AI SDK, billed via your Vendo balance.
let token = try await vendo.token("gemini")

// Use `token` wherever you would have used the provider's API key.
print(token)
```
  </Tab>
</Tabs>

## Learn more

- [Concepts: connections & integrations](/docs/concepts) — how connections, bindings, and credentials fit together.
- [Build a tool: SDK](/docs/build-a-tool) — full `vendo.token` and `vendo.data.execute` semantics.

- [Google Gemini API docs](https://ai.google.dev/api)
