---
title: Supabase
description: Manage Supabase projects (admin)
category: other
slug: supabase
logo: https://logos.composio.dev/api/supabase
---

{/* Generated by scripts/build-integrations.mjs. Hand-edit only the prose block between PROSE-START and PROSE-END markers. */}

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

{/* PROSE-START — hand-edited; the generator preserves this block */}
Supabase is wired into Vendo via the Composio bridge. Tool authors connect a tenant's Supabase account through Vendo's UI; once connected, the SDK exposes the credential at runtime so your tool can call the Supabase API on the tenant's behalf.
{/* PROSE-END */}

## Auth modes

| Profile | Default | How it works |
| --- | --- | --- |
| `composio_managed` (Composio-managed) | Yes | Connection brokered through Composio. Tenant connects via the embedded Composio OAuth flow; Vendo issues a Composio session at request time. |


## Environment variables

These are the env vars Vendo injects into your deployment at boot when this integration is bound.

| Variable | Source |
| --- | --- |
| `SUPABASE_ACCESS_TOKEN` | `credential.access_token` |


## Direct API

Supabase is called directly (no Vendo proxy intermediary). The SDK reads the injected credential from the environment and talks to Supabase's API host.

## Quickstart

<Tabs items={["Python", "TypeScript", "Swift"]}>
  <Tab value="Python">
```python
import vendo

# Returned token is the credential for this provider.
token = vendo.token("supabase")

# Use `token` wherever you would have used the provider's API key —
# pass it to the official SDK or set it as the env var the SDK reads.
print(token)
```
  </Tab>
  <Tab value="TypeScript">
```typescript
import { Vendo } from "@vendodev/sdk";

const vendo = new Vendo();
// Returned token is the credential for this provider.
const supabaseToken = await vendo.token("supabase");

// Use `supabaseToken` wherever you would have used the provider's API
// key — pass it to the official SDK or read it as the env var the SDK expects.
console.log(supabaseToken);
```
  </Tab>
  <Tab value="Swift">
```swift
import VendoSDK

let vendo = Vendo()
// Returned token is the credential for this provider.
let token = try await vendo.token("supabase")

// Use `token` wherever you would have used the provider's API key.
print(token)
```
  </Tab>
</Tabs>

## Learn more

- [Concepts: connections & integrations](/docs/concepts) — how connections, bindings, and credentials fit together.
- [Build a tool: SDK](/docs/build-a-tool) — full `vendo.token` semantics and resolution.

