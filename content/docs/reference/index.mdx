---
title: Reference
description: Generated SDK reference, the `vendo.yaml` schema, the HTTP API, and CLI reference.
---

Authoritative reference material:

- [**SDK**](sdk) — Python, TypeScript, and Swift surfaces generated from `surface.yaml`.
- [**vendo.yaml schema**](vendo-yaml-schema) — every field of the deploy-time manifest.
- [**HTTP API**](http-api) — the surface at `vendo.run/api/…` that SDKs and the CLI call under the hood.
- [**CLI**](cli) — the `vendo` command: login, dev, validate, ssh.

Reference pages are verifiable against the code. If a page disagrees with the source it points to, the source wins — please file an issue.
