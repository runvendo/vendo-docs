---
title: Reference
description: Generated SDK reference, the `vendo.yaml` schema, the HTTP API, and CLI reference.
---

Authoritative, generated-where-possible reference material: the SDK surface for Python, TypeScript, and Swift (generated from `surface.yaml`), the full `vendo.yaml` manifest schema, the HTTP API exposed at `vendo.run/api/…`, and the public CLI (when it ships).
