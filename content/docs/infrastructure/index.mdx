---
title: Infrastructure
description: What runs under your tool — runtime targets, subdomains, the proxy, credentials, logs, healthchecks, and isolation.
---

The infrastructure section makes the under-the-hood model navigable: which runtime each tool type targets, how subdomains and custom domains resolve, how the proxy worker meters third-party calls, where logs live and how long they're retained, the healthcheck contract, how secrets and env vars get injected at boot, scaling and resource limits, available regions, and the per-tenant isolation model.
